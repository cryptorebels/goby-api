{
  "name": "forms-api",
  "version": "0.2.0",
  "main": "server/server.js",
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "lint": "eslint server",
    "start": "node .",
    "dev": "NODE_ENV=development DB_HOST=localhost npm start",
    "ci-test": "NODE_ENV=test npm test -- --no-color",
    "test": "jest",
    "posttest": "npm run lint && nsp check",
    "postversion": "git push && git push --tags --no-verify"
  },
  "dependencies": {
    "compression": "^1.0.3",
    "cors": "^2.5.2",
    "helmet": "^3.0.0",
    "loopback": "^3.0.0",
    "loopback-boot": "^2.6.5",
    "loopback-component-explorer": "^5.0.0",
    "loopback-connector-mongodb": "^3.3.0",
    "serve-favicon": "^2.0.1",
    "strong-error-handler": "^2.0.0"
  },
  "devDependencies": {
    "eslint": "^4.0.0",
    "eslint-config-alvarium-nodejs": "^0.0.2",
    "eslint-config-loopback": "^8.0.0",
    "jest": "^21.2.1",
    "nsp": "^2.1.0"
  },
  "repository": {
    "type": "",
    "url": ""
  },
  "license": "UNLICENSED",
  "description": "forms-api",
  "jest": {
    "collectCoverage": true,
    "collectCoverageFrom": [
      "**/*.{js,jsx}"
    ]
  }
}
